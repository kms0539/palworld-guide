{
  "name": "palworld-guide",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "refresh": "node scripts/update-guide-data.mjs --root . && node scripts/build-public-guide.mjs --root .",
    "assets:sync": "node scripts/sync-visual-assets.mjs --root .",
    "preview": "node scripts/serve-static.mjs --root site --port 4179",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=20" }
}
