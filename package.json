{
  "name": "palworld-guide",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "refresh": "node scripts/update-guide-data.mjs --root . --public && node scripts/build-public-guide.mjs --root .",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=20" }
}
